# Project Rules

- Short and simple math (e.g., single variables, short expressions) is okay inline. Once a math expression starts lengthening or becomes complex, it must be written in display math blocks (`$$...$$`) rather than inline (`$...$`).
